export class StepVariantDto {
}
export class NotificationStepDto extends StepVariantDto {
}
export function isStepCreateBody(step) {
    return step && typeof step === 'object' && !step._id;
}
export function isStepUpdateBody(step) {
    return step && typeof step === 'object' && !!step._id;
}
